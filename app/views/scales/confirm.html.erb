<p>以下の内容でよろしいですか？</p>
<form method="post" action="/scales/create">
    <p>スケール名</p>
    <p><input type="text" name="scale_type" value="<%= @scale_type %>" disabled>スケール</p>
    <input type="hidden" name="scale_type" value="<%= @scale_type %>" >
    <input type="hidden" name="select_notes_ids" value="<%= @select_notes_ids %>" >
    <% notes.each do |note| %>
        <input 
            type="checkbox"
            name="<%= note.id %>"
            value="<%= note.id %>"
            disabled
            <% if @select_notes_ids.include?(note.id.to_s) %>
                checked 
            <% end %>
        >
        <%= note.id %>
    <% end %>
    <p><input type="submit" value="登録"></p>
</form>